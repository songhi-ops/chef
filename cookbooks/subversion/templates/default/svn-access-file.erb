
[groups]
<% 
@repo_list.each do |repo|
    users = repo[1][0]["username"]
    size = repo[1].length - 1
    repo[1][1..size].each do |user|
        users = users + ', ' + user["username"]
    end
%>
<%= repo[0] %> = <%= users %>
<%
    users = ''
end%>

<%
@repo_list.each do |repo|
%>
[<%= repo[0] %>:/]
@<%= repo[0] %> = rw
<%= %>
<% end %>

